[project]
name = "aatt-snowflake"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "chromadb>=1.3.5",
    "pypdf2>=3.0.1",
    "python-docx>=1.2.0",
    "ragas==0.3.7",
]

[tool.setuptools]
packages = ["ps_rag"]